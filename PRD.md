# PRD — 교실 아케이드 (가칭)

학급 실시간 멀티플레이 미니게임 플랫폼 · 작성일 2026-08-09 · v1.0 (초안)

---

## 1. 한 줄 정의

교사가 TV에 방을 열고, 학생들이 QR코드를 찍어 폰으로 입장해서, 여러 실시간 미니게임 중 하나를 골라 다 같이 플레이하는 **학급용 파티게임 플랫폼**.

- 벤치마크: Jackbox Party Pack(입장 방식), Kahoot(교실 운영 모델), 크레이지 아케이드(액션 감성)
- 기존 "랜덤뽑기"류 도구가 허무한 이유(결과가 즉시 나오고 학생이 아무것도 안 함)를 실시간 조작·경쟁·연출로 해결한다.

## 2. 목표와 성공 기준

| 목표 | 성공 기준 |
|---|---|
| 학급 전체가 동시에 즐긴다 | 한 방 **30명** 동시 접속에서 끊김 없이 플레이 |
| 준비 시간 제로 | 교사가 방 열고 학생 전원 입장까지 **2분 이내** |
| 실시간 체감 | 입력→상대 화면 반영 지연이 교실 와이파이 기준 **150ms 이내** |
| 확장 가능한 플랫폼 | 새 게임 추가 시 로비·입장·결과 코드는 재사용, 게임 로직만 작성 |
| 무료 운영 | 서버·호스팅 비용 0원 (무료 티어) |

## 3. 사용자

- **교사(호스트)**: 교실 TV/빔에 호스트 화면을 띄운다. 방 생성, 게임 선택, 시작/종료 제어. 회원가입 없음.
- **학생(플레이어)**: 개인 폰 또는 태블릿. QR 스캔 → 닉네임 입력 → 즉시 입장. 설치·로그인 없음.
- 대상 학년: 초3~고3 (조작이 단순해 저학년도 가능, 게임별 난이도로 조절)

## 4. 핵심 사용자 흐름

```
[교사] 사이트 접속 → "방 만들기" → TV에 QR + 방코드(4자리) 표시
[학생] QR 스캔(또는 코드 입력) → 닉네임 입력 → 로비 입장 (TV에 이름 실시간 표시)
[교사] 게임 선택 → 게임별 규칙 화면(15초) → 카운트다운 → 플레이
[전원] 플레이 (학생: 조작 화면 / TV: 전체 관전 화면)
[TV]  결과 연출 (순위, 우승자 하이라이트) → 로비 복귀 → 다른 게임 선택
```

- 한 방에서 게임을 바꿔가며 연속 플레이 가능 (방 유지)
- 게임 도중 새 학생 입장 시: 대기석에서 관전, 다음 판부터 참여

## 5. 플랫폼 공통 기능 (게임과 무관하게 재사용)

| 기능 | 내용 |
|---|---|
| 방 시스템 | 4자리 숫자 코드, 동시 다중 방 지원, 마지막 접속 30분 후 자동 소멸 |
| QR 입장 | 호스트 화면에 `https://…/join?room=1234` QR 자동 생성 |
| 닉네임 | 학생 입력(최대 8자) + 금칙어 필터 + 중복 시 숫자 부여. 실명 강제 없음 |
| 호스트 제어 | 게임 선택·시작·강제종료, 특정 플레이어 퇴장, 전원 준비상태 표시 |
| 관전(TV) 모드 | 게임 전체 상황을 보여주는 별도 렌더링. 결과 연출 포함 |
| 재접속 | 학생 폰이 잠기거나 새로고침해도 같은 닉네임으로 이어서 참여 (세션 토큰) |
| 결과·리더보드 | 판별 순위 + 방 누적 포인트(선택). 저장은 방 수명 내에서만 |
| 사운드 | TV에서만 BGM·효과음 재생 (학생 폰은 무음 — 교실 소음 방지) |

## 6. 게임 라인업

### 6.1 게임 분류 기준

- **A형 (이동 액션)**: 조이스틱 이동이 실시간 동기화. 기술적으로 가장 무거움. 권장 인원 4~16명(맵 크기 조절로 30명까지).
- **B형 (타이밍·판정)**: 버튼 입력의 순서/타이밍을 서버가 판정. 30명 이상도 가벼움.
- **C형 (입력·제출)**: 텍스트/선택 제출 후 동시 공개. 인원 제한 사실상 없음.

### 6.2 1차 출시 (MVP) — 3종

| # | 게임 | 형 | 규칙 요약 | 인원 |
|---|---|---|---|---|
| 1 | **폭탄 피하기 서바이벌** | A | 격자 맵에서 조이스틱 이동. 랜덤 위치에 폭발 예고(1초) 후 폭발. 맞으면 탈락, 갈수록 빨라짐. 최후 1인 승 | 4~30 |
| 2 | **눈치게임** | B | 1부터 순서대로 버튼으로 숫자 선점. 동시(50ms 이내) 입력 시 둘 다 탈락. 서버 도착 순서로 판정 | 5~40 |
| 3 | **반응속도 대결** | B | 전원 화면 빨강 → 랜덤 타이밍에 초록 → 탭. 측정은 폰 로컬, 결과만 제출. 최속·최저 발표 | 2~40 |

MVP 선정 이유: A·B형 각 1개로 플랫폼의 기술 스펙(이동 동기화 + 순서 판정)을 모두 검증하면서, 반응속도 대결로 "가장 만들기 쉽고 확실히 재밌는" 안전판을 확보.

### 6.3 2차 확장 — 4종 (MVP 검증 후)

| # | 게임 | 형 | 규칙 요약 |
|---|---|---|---|
| 4 | **물풍선 아레나** (크아식) | A | 폭탄 설치·십자 폭발·아이템. MVP 1번의 엔진 확장 |
| 5 | **실시간 낱말 배틀** (훈민정음식) | C | 제시된 초성/주제로 단어를 먼저 입력한 사람이 득점. 서버가 중복·유효성 판정. 국어 수업 연계 |
| 6 | **색 땅따먹기** | A | 이동하면 자기 색으로 칠해짐. 제한시간 내 최다 면적 승. 팀전 가능 |
| 7 | **폭탄 돌리기** | B | 랜덤 타이머 폭탄을 탭으로 다른 사람에게 패스. 종료 시 소지자 벌칙 |

### 6.4 게임 추가 규격 (플랫폼 조건)

새 게임은 다음 인터페이스만 구현하면 추가된다:
- 서버: `onJoin / onInput / tick / isGameOver / getResult`
- 클라: 플레이어 조작 화면 1개 + TV 관전 화면 1개
- 로비, 입장, 카운트다운, 결과 연출은 플랫폼이 공통 제공

## 7. 기술 아키텍처

```
[학생 폰]  단일 HTML/JS (GitHub Pages) ──┐
[교사 TV]  같은 HTML의 호스트 모드 ──────┼── wss:// ──> [Node.js + ws 서버 (Render)]
                                          │                 · 방 관리 / 게임 루프
                                          │                 · 20Hz 틱, 상태 방송
                                          └                 · 판정 (서버 권위)
```

| 항목 | 결정 | 비고 |
|---|---|---|
| 서버 | Node.js + `ws` 라이브러리, 단일 프로세스 | 프레임워크 없이 경량. 방=인메모리 객체, DB 없음 |
| 호스팅 | Render 무료 (싱가포르 리전) | 왕복 70~100ms. 부족하면 Fly.io 도쿄(30~40ms)로 이전 — 코드 수정 없이 이전 가능하게 작성 |
| 프론트 | 순수 HTML/CSS/JS 단일 페이지 + Canvas | 빌드 도구 없음(기존 제작 원칙 유지). GitHub Pages 배포 |
| 통신 | WebSocket(wss, 443 포트) | 학교망 방화벽 통과를 위해 443 필수. JSON 메시지 |
| 동기화 | 서버 20Hz 상태 방송 + 클라 보간(interpolation) | A형 게임. B·C형은 이벤트 단위 전송 |
| 판정 | 전부 서버에서 (서버 권위 모델) | 치팅·시비 방지. 반응속도 게임만 로컬 측정 예외 |
| 시계 동기화 | 접속 시 서버-클라 시각 오프셋 측정 (NTP식 왕복 3회) | 카운트다운·초록불 동시 점등에 사용 |
| 조작 UI | 가상 조이스틱(좌) + 액션 버튼(우), 세로/가로 대응 | A형. B·C형은 대형 버튼/키패드 |
| QR 생성 | 클라이언트 JS로 생성 (외부 API 미사용) | |
| 폰트/디자인 | Pretendard(jsDelivr), ~/.claude/design-guide.md 학생 프리셋 | |

### 용량 산정 (30명 방 기준)

- 상태 방송: 30명 × 20Hz × ~1KB = 약 600KB/s (서버 송신) — 무료 티어로 충분
- Render 무료 인스턴스(512MB)로 동시 3~5개 방 무리 없음
- 서버 슬립(15분 미사용) 대응: 호스트 화면 접속 시 자동 웨이크 + "서버 깨우는 중" 안내(최대 30초)

## 8. 비기능 요구사항

- **개인정보**: 수집 항목 없음. 닉네임은 서버 메모리에만 존재, 방 소멸 시 삭제. 실명 입력 유도 금지(안내 문구 "별명을 입력하세요")
- **접근성 최소선**: 색약 대응(색+모양 이중 표시), 진동 피드백(지원 기기), 터치 영역 44px 이상
- **성능**: 학생 폰 기준 보급형 안드로이드(3~4년 전 기종)에서 30fps 이상
- **네트워크 열화 대응**: 1초 이상 끊기면 "재접속 중" 표시 후 자동 복귀. 게임은 서버에서 계속 진행
- **호스트 이탈**: 교사 화면이 끊겨도 게임은 진행, 재접속 시 관전 화면 복원

## 9. 로드맵

| 단계 | 내용 | 산출물 |
|---|---|---|
| M1 | 플랫폼 뼈대: 서버·방·QR 입장·로비·재접속 | 로비까지 동작하는 배포본 |
| M2 | 반응속도 대결 (가장 단순한 게임으로 파이프라인 검증) | 첫 플레이 가능 버전 |
| M3 | 눈치게임 (서버 순서 판정 검증) | |
| M4 | 폭탄 피하기 서바이벌 (이동 동기화·보간·조이스틱) | MVP 완성 |
| M5 | 현장 테스트(실제 학급) → 지연·조작감 피드백 반영 | 정식 v1 |
| M6+ | 2차 게임 확장 (물풍선 아레나, 낱말 배틀, …) | |

## 10. 리스크와 대응

| 리스크 | 대응 |
|---|---|
| 학교망이 WebSocket 차단 | wss 443 사용으로 대부분 통과. 차단 학교 대비 폴링 폴백은 **만들지 않음**(액션 게임 성립 불가) — 대신 교사 핫스팟 안내 |
| Render 슬립으로 첫 접속 지연 | 호스트 접속 시 자동 웨이크 + 로딩 안내. 수업 전 미리 열기 안내 문구 |
| 30명 동시 접속 시 A형 게임 렌더 부하 | 맵·오브젝트 수를 인원에 따라 스케일, 폰 화면은 자기 주변만 렌더(뷰포트) 옵션 |
| 폰 없는 학생 | 2인 1폰 팀 플레이 허용(닉네임에 팀 표시), TV 관전만으로도 참여감 있는 연출 |
| 지연 시비("나 눌렀는데!") | 서버 권위 판정 + 판정 결과에 ms 단위 기록 표시로 납득시키기 |
| 이름 장난(욕설 닉네임) | 금칙어 필터 + 호스트 강제 퇴장 + 호스트 화면에서 닉네임 터치로 즉시 제거 |

## 11. 범위 제외 (v1에서 안 하는 것)

- 회원가입·로그인·학급 관리 (방 코드만으로 운영)
- 점수 영구 저장·통계 (방 수명 내 리더보드만)
- 전국 매칭·공개 방 (교실 내 사적 방만)
- 네이티브 앱 (웹만)
- 폴링 폴백 (WebSocket 전제)

## 12. 오픈 질문 (구현 전 결정 필요)

1. 서비스 이름 (가칭 "교실 아케이드")
2. 누적 포인트제를 넣을지 (판마다 리셋 vs 방 단위 누적 리더보드)
3. 팀전(모둠 대항) 모드를 MVP에 넣을지, 2차로 미룰지
4. 저학년(초1~2)용 조작 간소화 모드 필요 여부

# 교실 아케이드 (Class Arcade) — 인수인계 문서

> 다른 계정/세션에서 이어서 작업하기 위한 전체 정리. 이 파일 하나만 읽으면 프로젝트 전체를 파악할 수 있다.
> 마지막 갱신: 2026-09-03 (v2 비주얼 층)

---

## 1. 한 줄 요약

교사가 TV에 방을 열고, 학생들이 폰으로 QR을 찍어 접속해 **한 방에 모여 실시간 미니게임을 즐기는 학급용 파티게임 플랫폼**. 잭박스/카훗 + 크레이지 아케이드 감성. 게임 22종, 픽셀 아트, 최대 40명.

## 2. 접속 주소 (라이브 운영 중)

- **게임 주소**: https://class-arcade-i6xj.onrender.com
- **커스텀 도메인**: `arcade.kimju.kr` (가비아 CNAME → onrender, 연결 완료). `game.kimju.kr` 추가 작업은 사용자가 진행 중일 수 있음.
- **GitHub**: https://github.com/kimju1416/class-arcade (공개, kimju1416 계정)
- **호스팅**: Render 무료 티어(싱가포르). GitHub push하면 자동 재배포.
- **아카이브 카드**: kimju.kr(저장소 kimju1416/ac)에 "Class Arcade (교실 아케이드)"로 등록됨. 게임 수 바뀌면 카드 설명의 "게임 N종" 숫자 갱신 필요.

## 3. 기술 구조

```
[학생 폰]  단일 HTML/JS ──┐
[교사 TV]  같은 HTML ──────┼── wss:// ──> [Node.js + ws 서버 (Render)]
                          │                 · 방 관리 / 20Hz 게임 루프 / 서버 권위 판정
                          └                 · 정적 파일 서빙도 겸함
```

- **server.js** (~1700줄): Node.js + `ws`. 방 관리 + 게임 10종 로직 + public/ 정적 서빙. DB 없음(전부 인메모리).
- **public/index.html** (~2000줄): 교사 TV 모드 + 학생 폰 모드 겸용 단일 파일. Canvas 렌더링, WebSocket, 20Hz 상태 수신 + 보간.
- **public/sprites/**: gpt-image로 만든 픽셀 아트 24개(합계 ~700KB).
- 빌드 도구·프레임워크 없음. `npm install`(ws 하나) 후 `node server.js`.

### 로컬 실행
```bash
npm install
node server.js         # http://localhost:3000
# 테스트: PORT=3100 DEBUG_KEY=testkey node server.js  → /debug?key=testkey 로 방 상태 JSON
```
학생 흉내: 탭 하나 더 열고 `http://localhost:3000/?room=코드&fresh=1` (fresh=1은 같은 브라우저 탭들이 서로 딴 사람으로 입장하는 테스트 옵션)

## 4. 게임 10종

| 키 | 이름 | 조작 | 규칙 요약 |
|---|---|---|---|
| bomb | 폭탄 피하기 | 조이스틱 | 떨어지는 폭탄 회피, 최후 1인. 갈수록 빨라짐 |
| tag | 감염 술래잡기 | 조이스틱 | 좀비(8명당1) 닿으면 감염, 좀비 1.08배속. 60초 |
| coin | 동전 줍기 | 조이스틱 | 동전 수집(금화3점), 💣폭탄 밟으면 0원. 60초·탈락없음 |
| mos | 모기 잡기 | 화면 탭 | 파리채로 모기 잡기(황금3점). 60초·탈락없음 |
| claw | 인형 뽑기 | 버튼 | 좌우 왕복 집게를 버튼으로 멈춰 뽑기(곰1·토끼3·유니콘5점). 60초 |
| race | **레이싱** | 조이스틱+부스터 | 왼손 핸들+오른손 부스터(3회). S자 서킷+물/기름 함정. 3랩. **사용자 최애** |
| gala | 갤러그 | 조이스틱+발사 | 협동 슈팅. 왼손 이동+오른손 발사(홀드연사). 웨이브 무한 상승. 죽으면 끝 |
| dodge | 탄막 피하기 | 조이스틱 | 링/조준/회오리 탄막 회피. 한 발 즉사. 혼자 남아도 계속 — 전멸/시간 종료(150초)까지 진행. 시간 비례 난이도↑ |
| word | 낱말 빨리치기 | 텍스트 | TV 제시어 빨리 정확히 입력. 5라운드·순서점수(10/8/6…) |
| cho | 초성 퀴즈 | 텍스트 | 초성(ㅇㅇㅅㅋㄹ→아이스크림). **전 카테고리 사전 대조** — 초성이 맞아도 사전에 없는 단어는 오답(음식·동물·사물장소는 확장 사전 1,818단어). 중복단어 금지. 5라운드 |
| sumo | 스모 밀치기 | 조이스틱+대시 | 속도 물리+충돌 넉백. 대시(쿨 2.4초)로 밀치면 1.5배. 링이 15초 후부터 55%까지 축소. 링 밖=장외, 최후 1인 |
| chair | 의자 뺏기 | 조이스틱 | music(5~10초 랜덤, TV에 멜로디)→grab(9초, 의자=생존자×0.66) 반복. 의자 근접 선착순 점유, 못 앉으면 탈락. 음악 종료 시각은 절대 클라에 안 보냄 |
| sadari | 사다리 복불복 | 탭 | **칸 수는 인원수대로(최소 3·최대 8, 라운드마다 재계산)**·13행. 상품은 전체 배열에서 칸 수만큼 고르게 뽑아 셔플(황금 +10과 💣-5는 항상 포함). pick 11초(칸별 인원수만 공개)→reveal 6.5초(가로줄·경로 공개는 이때만 — 미리 보내면 치팅). 5라운드 합산 |
| quiz | 퀴즈쇼 | 4색 버튼 | 카훗식. QUIZ_DATA 6주제×20문항(기초·아이스브레이킹), opt {cat,count,custom}로 주제·문제수·커스텀(내문제: 붙여넣기 파싱 parseCustomQ, 최대 20개·글자수 캡·3개 미만 폴백, maxPayload 16KB로 상향). 정답 100+속도보너스 최대 100. 보기 순서 판마다 셔플, 정답·분포는 reveal에만 전송(컨닝 방지) |
| ox | OX 서바이벌 | 조이스틱 | OX_DATA 55문항(O28·X27). show 12초(이동)→reveal 3.5초. 정답구역: O=왼쪽 절반. 오답 탈락, 전원 오답이면 전원 부활(oxRevived). 최대 15문제 or 1명 남으면 종료 |
| timing | 10초를 잡아라 | 화면 탭 | 목표 0.5초 단위 랜덤(라운드별 4~6/6~9/9~12/12~16초). ready 3.5초→run(시계는 min(3초, 35%)만 보이고 가림)→reveal 6.5초. 판정은 stop 메시지 서버 도착 시각(조작 불가). 점수 100-오차50ms당1, 오차≤50ms +50 보너스. 4라운드 합산 |
| pirate | 통아저씨 | 드래그+탭 | 통 24칸(8열×3줄 원통), 위험 칸 라운드1 4개→+1씩. pick 11초→reveal(1.6초에 펑·탈락 적용). 생존자 12% 이하 또는 8라운드에 종료. 폰은 드래그로 통 회전(pirRot) |
| run | 설원 러너 | 조이스틱(플릭)+점프 | 뒤에서 보는 유사 3D 러너(펭귄). 3레인·스틱 플릭 1회=1칸·점프 620ms 포물선(높이 95). 낮은 블록=점프 통과(높이 42+), 높은 기둥=레인 변경만. 코인 8개=피버 4초(1.6배+무적), 충돌=비틀거림 0.9초(0.35배)+무적·게이지 −3. 점수=거리m+코인×15, 탈락 없음, 80초. 코스는 방마다 랜덤(`buildRunCourse`)·phase에 course 통째 전송, 판정은 전부 서버 스윕(obIdx/coinIdx 포인터로 prevDist→dist 구간만 검사) |
| simon | 사이먼 가라사대 | 4색 패드 | show(패턴은 `simon_seq`로 **호스트에게만**·TV가 발광+SFX.tone)→input(서버가 skey 대조·틀리면 즉시 탈락)→reveal 반복, 음 3개부터 +1씩. 전원 탈락 시 전원 부활(siRevived). 최후 1인/14라운드 종료, 순위=탈락 라운드 |
| chimp | 침팬지 테스트 | 화면 탭 | memo(숫자는 `chimp_nums`로 호스트에게만)→input(pick=셀 탭, 1부터 순서대로·+10/라운드 완주+15·틀리면 라운드 실패, 이미 맞힌 칸 재탭은 무시)→reveal(nums 전원 공개). 8라운드(숫자 4→11), 점수제 endScoreGame |
| flash | 순간 포착 | 4색 버튼 | memo(장면은 `flash_scene`으로 호스트에게만 2.6초)→show(문제·4지선다 12초, 기존 answer 핸들러 공용)→reveal(정답+분포+장면 재공개). 8문제, 정답 100+속도 보너스. quiz와 state 필드 미러 |
| pairs | 같은 그림 찾기 | 화면 탭 | 10쌍 20장 전원 동일 배치, 각자 자기 보드. **카드 앞면은 `pairs_flip`으로 뒤집은 본인에게만**(남의 state를 훔쳐봐도 면을 모름). 두 장 뒤집으면 650ms 후 서버 판정, 점수=쌍×100−시도×2+완주 시 남긴 초×4. 75초 or 전원 완주 시 종료 |

- **공통 룰**: bomb/gala/dodge는 "죽으면 부활 없이 끝", 나머지는 탈락 없거나 감염/점수제.
- **게임 추가 규격**: server.js에 `GAME_KEYS` 배열 + startGame 초기화 + `게임Tick` 함수 + sendState 분기 + (필요시)입력 핸들러. 클라는 `GAMES_META` 카드 + draw() 렌더 분기 + 입력 분기. 로비·입장·카운트다운·결과·트로피는 플랫폼 공통 제공.

## 5. 핵심 설계·주의사항 (건드리기 전 반드시 읽기)

### 서버
- **서버 권위 모델**: 판정 전부 서버, 클라는 입력만 전송. 학생이 콘솔로 조작 못 하게 전 핸들러에 gameType/state/소유권 가드 있음.
- **시계 동기화**: 접속 시 ping 3회 평균으로 서버-클라 시차 측정. 카운트다운·동시 점등에 사용.
- **재접속**: 학생은 localStorage 토큰으로 원래 자리 복귀, 교사는 sessionStorage로 방 복귀. 게임 중 끊기면 유예 후 tick에서 탈락.
- **레이싱 트랙**: `buildTrack()`이 제어점 14개를 카트무-롬 스플라인으로 이어 만듦(1600×1050). 완주는 "출발점부터 정확히 3바퀴 거리"로 판정(격자 위치 무관 공정). S자 지름길은 한 틱 인덱스 급점프(fwd>8) 시 무효.
- **총알은 평탄 배열**([x1,y1,x2,y2…])로 전송해 용량 절약. gala eb 상한 200, dodge 상한 130.
- **tick try/catch**: 10회 연속 예외 시 방을 로비로 강제 복귀(uncaughtException 핸들러도 있음).
- **maxPayload 4KB, rate limit 60msg/s, IP당 방 제한 등 보안 가드 있음** (realtime-game-server-security 메모 참고).

### 클라이언트
- **phase 재수신 처리**: 같은 게임 playing 재수신(레이싱 1등 완주로 남은시간 30초 단축)은 시간만 갱신하고 화면·입력 안 건드림. 안 그러면 주행 중 전원 조이스틱 풀림(실제 있던 버그).
- **캐릭터 30색**: 흰 몸통 스프라이트 1장을 `multiply` 합성으로 색만 바꿔 재사용(tintSprite). 얼굴은 코드로 그림.
- **양손 조작**: 왼쪽 62% 조이스틱, 오른쪽 버튼(부스터/발사). 갤러그 연사는 setPointerCapture+firePid로 버튼 밖에서 떼도 정지.
- **사운드 정책**: 전역 사운드(카운트다운·GO·게임 이벤트 중계·팡파레·BGM)는 **TV 전용**. 학생 폰(ca_stusnd, 기본 끔)은 소리를 켜도 **본인 이벤트만** — 내 탈락·내 감염·내 획득(동전/모기/인형/격추)·내 정답(퀴즈·순간포착·낱말)·내 짝맞춤·내 완주·내 줄클리어·버튼 손맛(부스터/집게/대시)·사이먼 내가 누른 음. 여러 폰이 전역 사운드를 같이 울리면 교실에서 뒤섞인다. 새 게임에 사운드를 넣을 때 전역 트리거는 반드시 `isHost` 가드, 개인 피드백은 `!isHost && 내 행` 기준.
- **확인창은 `uiConfirm()`** — 브라우저 `confirm()`은 OS 창이라 게임 톤을 깬다. `uiConfirm(제목, 설명, {ok, cancel, danger, icon})`가 Promise를 반환(Esc·바깥 탭=취소, Enter=확인). 게임 중단은 `confirmAbort()` 헬퍼. **Playwright 테스트에서 `page.on('dialog')`는 이제 불필요 — `#modalOk`를 클릭할 것.**
- **보간**: draw()가 스냅샷 2개(a→b) 사이를 t로 보간. **커스텀 렌더러 추가 시 반드시 b.m 기준으로 그릴 것**(latest 섞으면 위치 튐 — 레이싱에서 실제 겪음).
- **v2 연출 층 `FX` (2026-09-03)**: 전 게임 공용. `FX.burst/ring/pop/shake/flash/banner/feedAdd`. **게임별 코드를 고치지 않고 `fxDiff(latest)`가 서버 상태 diff(탈락·감염·득점·폭탄)를 감지해 자동 발화**한다 — 행 구조 `[id,x,y,alive,waiting,extra]`를 쓰는 아레나 게임(`FX_ARENA`)만 위치 연출, 나머지는 배너·피드만. 흔들림은 `#cv`의 CSS transform, 번쩍임은 `#fxFlash` DOM이라 전용 렌더러 게임(러너·카트·꼬리별)에서도 먹는다. 새 게임을 추가하면 `enterGameView`가 `FX.reset()`을 부르니 별도 처리 불필요. 배경 캔버스 `#bgfx`(BG 모듈)는 게임 캔버스가 뜨면 스스로 쉰다.
- **이모지 반응**: 학생 폰 로비·결과 화면의 `react-bar` → 서버 `react {e:0~7}` → **TV에게만** 중계(`#reactLayer`에 떠오름). 서버가 로비·결과 상태에서만 받고 학생당 0.6초 간격(`REACT_MIN_MS`)으로 제한. 종류를 늘리면 클라 `REACTS`와 서버 `REACT_MAX`를 같이 바꿀 것.
- **로비 🎲 랜덤 뽑기**: `spinRoulette()` — 보이는 카드(`.game-card[data-key]`)만 대상. 카드에 `data-key`가 있으니 테스트는 인덱스 말고 키로 클릭할 것.

### 함정 (실제로 당했던 것들)
- **bash heredoc + python 문자열의 `\n`이 실제 개행으로 들어가 JS 깨짐** → 한글 UI 문구는 Edit 도구로 직접 수정.
- **스프라이트 내부에 마젠타(#FF00FF)가 있으면 크로마키로 뚫림** → 캐릭터 눈처럼 뚫리면 안 되는 곳은 마젠타 피해 생성.
- **행 파생 const는 구조분해 직후 선언**(중간 삽입하면 TDZ 크래시).
- **Pretendard는 Google Fonts에 없음** → jsDelivr 서브셋 CDN 사용.

## 6. 개발·검증 워크플로우

- **이미지 생성**: gpt-image MCP(`generate_image`, engine auto). 마젠타 배경에 단일 오브젝트로 생성 → `scratchpad/build-sprites.js`(Playwright 캔버스로 크로마키 제거·크롭·축소)로 최적화 → `public/sprites/`에 저장.
- **검증**: Playwright로 봇 여러 명 접속시켜 자동 플레이. 전용 설치본 없어서 `프로젝트/youcall-promo/node_modules/playwright` require. 스크린샷은 Read로 눈 확인.
  - 회귀: `scratchpad/regression.js` (10종 시작·조작·중단)
  - 게임별: race-test.js, gala-test.js, fix-verify.js 등 (scratchpad는 세션 소멸 — 패턴은 아래)
  - 봇 조향(레이싱): 서버 트랙 수식을 테스트에 복사해 가까운 중심선 +8 앞 지점으로 이동.
- **배포**: `git push` → Render 자동 재배포(1~2분). 서버 코드는 클라로 서빙 안 되니 grep으로 배포확인 불가 → 라이브 동작(스모크)으로 확인.
- **Render 무료 함정**: WebSocket은 트래픽으로 안 쳐서 15분 뒤 잠듦 → 클라 25초 ws핑 + 호스트 4분 /health fetch로 방지. 첫 접속만 30초 웨이크.

## 7. 히스토리 (이 순서로 만들어짐)

1. MVP: 방·QR입장·폭탄 피하기 → 2. Render 배포 → 3. 조이스틱 터치 수정(touch-action) → 4. 게임 4종 추가 → 5. 초성퀴즈 → 6. 사운드(WebAudio 합성, 교사 화면만)·교사 PC 대형 레이아웃 → 7. 픽셀 아트 전면 적용 → 8. 레이싱 → 9. 레이싱 대개편(함정·미니맵) → 10. 갤러그·탄막 → 11. 디자인 보완(로고·트로피) → 12. 코드 검수 반영(버그 수정).

## 8. 다음 할 일 후보 (사용자와 논의된 것)

- ~~사용자가 레이싱을 제일 좋아함. 비슷한 "조종+경쟁" 게임 요청 중~~ → **완료(2026-08-12)**: 🏃 설원 러너(run) — 사용자가 러닝게임 스샷을 주며 직접 요청("앞으로 달려가는 화면, 좌우+점프, 코인"). 남은 후보: 🏂 다운힐(스키프리식), 🚁 동굴 탈출, 🍎 열매 서리, 🎣 낚시.
- 기타 논의된 것: 무궁화 꽃이 피었습니다, 물풍선 아레나(진짜 크아식), 숫자 사냥(수학연계), 색 땅따먹기(팀전), 팀전 모드, 아이템 박스(마리오카트식).
- ~~게임 10개 넘어가면 로비에 분류 탭 필요~~ → **완료(2026-08-12)**: 전체/서바이벌/점수 경쟁/퀴즈·두뇌/복불복 탭(`GAME_CATS`/`GAME_CAT_OF`, 한 게임이 복수 분류 가능, 선택 탭은 localStorage `ca_gtab`에 기억). 같은 날 퀴즈쇼·OX·10초의 카드 스프라이트 누락(quiz/ox/timing.png 404 → 이모지 폴백)도 생성해서 해결.
- **밸런스는 실제 학급에서 한 번 돌려본 뒤 조정**하는 게 정확함(아직 봇 테스트만 함).

## 9. 관련 파일

- `PRD.md` — 초기 기획서
- `README.md` — 사용법·Render 배포 안내(초보자용)
- `server.js` / `public/index.html` — 전부

---

**요약**: 게임 10종 완성·라이브 운영·버그 검수까지 끝난 안정 상태. 다음은 사용자 요청대로 "레이싱류 조종 게임"(펭귄 슬라이드 등) 추가가 유력.

# 슈퍼스타 카트 — 품질 업그레이드 계획

- 대상: `public/kart/` + `kart-server.js` (game.kimju.kr/kart, 2026-09-23 ad72516 기준)
- 용도: 이 문서를 보고 다른 세션(오퍼스)이 하나씩 구현한다. 항목마다 **왜 → 무엇을 → 어떻게(파일·함수·값) → 확인법**을 적었다.
- 지키는 것: 순수 HTML/JS(three r186), 이미지는 코덱스 한도가 풀릴 때만, 라이브는 Render 자동배포(main push). 검사 스크립트는 `test/kart/README.md`.

## 0. 지금 상태 진단 (솔직하게)

| 영역 | 지금 | 부족한 점 |
|---|---|---|
| 조작감 | 아케이드 물리, 드리프트 3단 미니터보, 로켓 스타트, 점프대 | 드리프트에 «무게»가 없다(옆으로 미끄러지는 감각 0, 카운터스티어 무의미). 카트끼리 부딪혀도 튕김이 없다. 슬립스트림 없음. 벽에 닿으면 «미끄러지듯» 붙어 감 |
| 아이템 | 7종, 앞뒤 분배 | 뒤로 던지기·뒤에 달고 다니기(방패)가 없다. 맞은 뒤 무적 시간이 없어 연속 피격. 아이템 맞는 «맛»(카메라·소리·슬로모)이 약함 |
| 코스 | 4개, 전부 벽으로 막힌 한 갈래 길 | 지름길·갈림길·물에 빠짐·낙하가 없어 3바퀴가 똑같다. 코스별 «기억에 남는 한 장면»이 없다(예: 무대 위를 달린다, 파도 위 다리) |
| 그림 | 3D 배경·차 5종·운전자 앞/뒤 2장 | 옆에서 보면 종이 인형(2방향뿐). 폰(q1)은 그림자·블룸 없음. 도로는 흰 선 두 줄뿐 |
| 카메라·연출 | 추격 카메라, 시상식 | 코스 소개 컷, 뒤돌아보기, 리플레이, 결승 순간 연출이 없음 |
| 소리 | Mixkit BGM 6·효과음 19, 합성 엔진음 | 엔진음이 «윙» 한 음. 다른 카트 소리·거리감 없음. 아이템·이벤트 소리가 얇음. 곡은 아무도 안 들어봄 |
| 멀티·교실 | 12명·순번제·관전·팀전 | 새로고침하면 레이스에서 영영 빠짐. 방장 폰이 잠들면 봇 정지. 몇 판 누적 점수(컵)가 없다. 빠른 채팅·이모지 없음 |
| 성능·안정 | PC 60fps, 헤드리스 폰 화질 58fps | **실제 학생 폰·태블릿에서 한 번도 안 돌려 봄.** 로딩 진행 표시 없음, 화면 꺼짐(wake lock) 미처리, 세로 화면 안내 없음 |

## 1. 우선순위 한 줄 요약

1. **실기기 실측 + 자동 화질 + 로딩 UX** — 3,000명이 폰으로 열기 전에 반드시. (0.5일)
2. **조작감(드리프트·충돌·슬립스트림)** — 게임의 «맛»을 결정. (1일)
3. **아이템 손맛(뒤로 던지기·방패·피격 연출·무적)** (0.5일)
4. **코스에 «장면» 넣기(지름길·물 빠짐·무대 위 주행·오버패스)** (1.5일)
5. **카메라·연출(코스 소개·결승·리플레이·뒤돌아보기)** (0.5일)
6. **소리 다시 하기(엔진·도플러·거리·타격)** (0.5일)
7. **멀티 안정(재접속·봇 이관·컵 모드·빠른 채팅)** (1일)
8. **코덱스 열리면: 8방향 캐릭터 스프라이트·초상화** (별도)

각 항목은 독립이라 순서를 바꿔도 된다. 단 1번은 무조건 먼저.

---

## 2. 항목별 계획

### 2-1. 실기기 실측·자동 화질·로딩 UX  ★★★

**왜** 지금까지 «폰 화질»은 PC 헤드리스에서 폰 창 크기로 잰 것뿐이다. 실제 갤럭시 A·아이패드·구형 크롬북에서 15fps면 아래 모든 개선이 헛일이다.

**무엇을**
- (a) 실측 표: 형님 폰·학교 태블릿·교실 PC 3대에서 코스 4개를 열어 `fps·로딩 시간·발열` 기록. 게임 안에 `?debug=1`일 때 좌상단 fps·프레임시간·해상도 배율 표시.
- (b) **동적 해상도**: 최근 60프레임 평균 프레임시간이 20ms(50fps)를 넘으면 `renderer.setPixelRatio`를 0.1씩 내림(하한 0.6), 13ms 아래면 올림(상한 기기 dpr). 블룸은 q2에서만. 폰 q1에서 잔디 밀도(`dense`)를 0.6→프레임시간 보고 0.35까지 추가로 낮춤(월드 재생성은 안 되니 `InstancedMesh.count`를 줄이는 방식).
- (c) **로딩 화면**: 진행 막대(지형→도로→소품→셰이더 굽기 5단계, `window.__kartProf` 그대로 활용)와 코스 팁 한 줄("드리프트를 길게 하면 분홍 불꽃!"). 첫 방문엔 «그래픽 준비 중(처음 한 번만 오래 걸려요)».
- (d) **wake lock**(`navigator.wakeLock.request('screen')`) 레이스 중, 세로로 잡으면 «가로로 돌려 주세요» 안내(강제는 안 함), iOS 오디오 언락(첫 터치에 `audio.init()` 이미 있음 — 확인만).
- (e) **자원 캐시**: `public/kart/sw.js` 서비스 워커로 `/kart/*`·`/fps/three*.js`·텍스처·효과음 캐시(BGM은 코스별 lazy). 두 번째부터 로딩 1초대. 판 번호는 `index.html`의 `?v=`와 같이 올린다([[cache-buster-must-bump]] 규칙).

**어떻게** `main.js` `frame()`에 프레임시간 링버퍼 → `qualityLevel()` 옆에 `adaptQuality()`; `world.js`는 `W.lod = { grass: im, trees: [...] }`로 InstancedMesh 참조를 돌려주고 count를 조절. 로딩은 `startRace()`의 `$('loadTxt')` 자리에 `<progress>`.

**확인** `test/kart/pw-mobile.cjs`에 프레임시간 강제 지연(`setTimeout` busy loop)을 넣어 배율이 내려가는지; 실기기 표는 사람이 적는다.

### 2-2. 조작감 — 드리프트·충돌·슬립스트림  ★★★

**왜** 마리오카트의 재미 절반은 «미끄러지면서 잡아 도는 감각»이다. 지금은 핸들 각도만 커지는 «빠른 회전»이라 드리프트가 그냥 커브 보정이다.

**무엇을 / 어떻게** (`physics.js` `stepKart`)
- **옆 미끄러짐(slip)**: 상태에 `k.vx, k.vz`(속도 벡터)를 두고 `k.h`(차체 방향)와 분리. 매 걸음 `grip`(도로 0.92, 오프로드 0.75, 드리프트 중 0.6)만큼 속도 벡터를 차체 앞 방향으로 당긴다: `v = lerp(v, fwd*|v|, grip*dt*k)`. 드리프트 중엔 차체가 안쪽으로 돌지만 진행은 바깥으로 흐르는 «드리프트 각»이 자연히 생긴다. 이동은 `x += vx*dt`.
- **카운터스티어**: 드리프트 중 반대 핸들은 각을 줄이고(안정) 같은 방향 핸들은 각을 키운다(지금 `inward` 계수를 실제 각도에 쓰기). 미니터보 충전은 «드리프트 각이 클수록» 빨리.
- **충돌 튕김**: `bump()`에서 위치만 밀지 말고 상대 속도 반사: `v += n * max(0, dot(vrel, n)) * (1 + 0.4) * wb/(wa+wb)` + 0.15초 조향 잠금 + 소리·카메라 흔들림. 무게 차이(캐릭터 `wgt`)가 여기서 처음으로 의미를 갖는다.
- **벽**: 현재 «위치 클램프 + 방향 보정»을 «법선 반사 + 감속 0.55 + 불꽃 스파크」로. 벽에 비비며 달리는 게 이득이 되지 않게 비비는 동안 최고속도 0.8배.
- **슬립스트림**: 앞 카트 뒤 2~10m·옆 2m 안에 1.2초 있으면 `boostT=0.7`(작은 부스터, 흰 바람줄 효과 `fx.add`). 봇도 자연히 이득.
- **오프로드 단계**: 연석(curb) 밟기는 감속 0, 잔디/모래는 0.55배(현재), 깊은 모래·물가는 0.4배 + 진동.
- 값은 `test/kart/sim3.mjs`로 한 바퀴 42초 안팎이 유지되는지 보고 맞춘다. 봇 AI(`ai.js`)는 속도 벡터 도입 뒤 `want` 각 계산이 `k.h` 기준이라 그대로 동작하지만, 드리프트 역산식(`eff = steer/1.28`)은 새 모델에 맞춰 다시 맞춘다.

**확인** `sim3.mjs` offPct<8%·wall<3·laps 10; 사람이 직접 몰아 «드리프트 중 옆으로 밀리는 느낌»이 나는지.

### 2-3. 아이템 손맛  ★★

- **뒤로 던지기·뒤에 달기**: 아이템 버튼을 «길게(0.25초)» 누르면 야구공/바나나를 카트 뒤에 달고 다닌다(방패: 뒤에서 오는 공을 막음). 떼면 발사, 브레이크(아래로 당김)+버튼이면 뒤로 던짐. 구현: `k.held = {kind, since}` → 뒤 1.6m에 메시 표시, 서버 메시지 `item`에 `dir:-1`, `hazardsTick`에서 held는 `h.by` 카트 뒤 위치를 매 프레임 따라감.
- **피격 후 무적 1.5초**(깜빡임) — 연속 피격 방지. `spinOut`에 `k.invT`.
- **피격 연출**: 맞는 순간 0.12초 슬로모(`dt*=0.35` 전역, 온라인은 내 화면만), 카메라 줌 인 5%, 화면 가장자리 붉은 플래시, 컨트롤러 진동(`navigator.vibrate(80)`).
- **아이템 룰렛**: 지금은 80ms마다 아이콘 교체. 룰렛 슬롯이 «위→아래로 돌다 느려지며 멈추는» 세로 스크롤 캔버스로.
- **새 아이템 1개(축구공)**: 앞 3명을 차례로 튕기며 지나가는 공(`hball` 로직에 `hits:3`). 7→8종. 분배 표는 12인 기준으로 다시(뒤 4명에게 스타·마이크 확률 ↑).
- **아이템 상자**: 먹으면 «쨍» + 상자 조각 튐(있음) + 2.2초 뒤 아래에서 «펑» 하고 재생성(스케일 0→1 튕김은 있음, 소리 추가).

### 2-4. 코스에 «장면» 넣기  ★★★

원칙: 코스마다 딱 한 가지 «이 코스 하면 그거!»를 만든다. 트랙 수학(`trackmath.js`)은 중심선 하나이므로 갈림길은 «옆으로 넓은 구간 + 지형 높이 차」로 표현하는 게 가장 싸다.

- **선셋 비치**: 벽 일부를 없애고 바다 쪽으로 열어 둔다(`def.open: [[u0,u1,side]]` 구간은 wall 안 그림, `lim` 무시). 모래사장으로 달리면 느려지고 물(y< sea+0.3)에 빠지면 «첨벙» → 2초 뒤 드론(구름 위 드론 메시)이 트랙 가운데로 되돌림(봇 복귀 코드를 사람에게도). 지름길: 해안 모래길이 안쪽 커브를 가로지르되 모래라 느려서 «드리프트 부스터가 있어야 이득」.
- **네온 시티**: 오버패스(트랙이 자기 위를 지나는 8자). `locate`는 hint 주변만 찾으니 교차가 가능하다. 필요한 것: 지형 높이는 두 도로 중 낮은 쪽으로(`ND/NY` 찍을 때 `Math.min`), 도로 메시는 그대로, 위 도로 아래에 기둥·난간. 위층 구간 높이 7m, 경사 6%.
- **벚꽃 캐니언**: 계곡을 건너는 **다리**(양옆 난간 없이 낙하 가능) + 다리 아래 강. 떨어지면 드론 복귀.
- **K-POP 아레나**: 트랙이 **무대 위를 지나가게** 재설계(런웨이가 트랙 일부: 무대 전면 경사로 ↑ 무대 위 8m 직선 ↓ 반대 경사로). 무대 위에선 관중 함성 볼륨 최대·조명이 카트를 따라옴(스팟 1개 `SpotLight` target=선두).
- 공통: **터널** 1곳(반원 튜브 + 안쪽 조명 + 소리 잔향은 생략), **뱅크 코너**(도로 롤: `tr.roll[i]`를 곡률에 비례해 두고 도로 리본 좌우 높이차·카트 `body.rotation.z`에 반영, 물리는 그립 +10%).
- 코스 소개 컷(2-5)과 함께 «장면»이 인트로 카메라에 잡히게 한다.

**확인** `tcheck.mjs`(겹침은 오버패스 구간만 허용 목록으로), `sim3.mjs`(봇이 물에 안 빠지는지 — 봇 주행선에 `open` 구간 회피 가중), 스크린샷.

### 2-5. 카메라·연출  ★★

- **코스 소개 컷** 4초: 코스 이름·한 줄 소개 카드(#center 아래 띠), 카메라가 «장면»→출발선으로 스윕(`camIntro` 대신 3점 스플라인 `CatmullRomCurve3`). 스킵 가능(탭).
- **결승 순간**: 결승선 통과 프레임에 0.4초 슬로모 + 카메라가 옆으로 돌며(현재 `fin` 회전 있음) 체크 깃발 스프라이트 흔들기 + 관중 함성 1.0.
- **뒤돌아보기**: 키 `R`/폰 «뒤» 작은 버튼 — 카메라를 앞쪽에 두고 뒤를 봄(아이템 던질 때 유용).
- **리플레이 20초**: 내 카트 위치를 이미 100ms마다 기록 중(`race.rec`). 결승 뒤 «다시 보기» 버튼 → 마지막 20초를 트랙사이드 고정 카메라 3곳에서 번갈아 재생(뒤·옆·헬기). 봇 위치는 기록 안 하니 «내 카트만» 리플레이(솔로 한정).
- **미니맵**: 코스 형태 위에 카트 점(있음) + 아이템 위험물(있음) + «내 위치 화살표» 회전.

### 2-6. 소리  ★★

- **엔진**: 지금 톱니파 한 음. 4단 기어처럼 rpm이 «올라갔다 툭 떨어지는» 곡선(`speed01 → gear = floor(speed01*4)`, `rpm = frac*…`), 부스터 땐 한 옥타브 위 + 노이즈. 봇 엔진음은 거리 30m 안에서만, `PannerNode`로 좌우 위치(three `AudioListener` 대신 WebAudio 직접).
- **도플러**: 스치는 카트의 pitch를 상대 속도로 ±8%.
- **타격**: 맞았을 때 «딩~» 뒤 낮은 «쿵»(두 소리 합성), 벽 스치면 «끼익»(drift.mp3 짧게).
- **음악 덕킹**: 아이템 맞을 때·결승 때 BGM 0.4로 0.5초, 마지막 바퀴는 `bgm-final`(있음).
- **곡 검수**: 형님이 코스 4곡을 직접 들어보고 결정(우리는 못 듣는다 — [[nexus-isles-audio]] 교훈). 후보를 코스별 2곡씩 `audio/alt/`에 받아 두고 설정에서 고를 수 있게.

### 2-7. 멀티·교실 안정  ★★★ (교실에서 쓰려면 필수)

- **재접속**: `localStorage`에 `{code, id, token}` 저장. 서버 `hello`에 `rejoin:{id,token}`이면 같은 `p.id`로 복귀(레이스 중이면 그 자리·기록 유지). 서버는 끊긴 뒤 60초 동안 자리를 보존(`p.ghost=true`, 봇이 대신 몰지 않음 — 그 자리에 멈춤).
- **봇 이관·정지 방지**: 방장 탭이 `visibilitychange` hidden이면 서버에 `{t:'tv-away'}` → 서버가 다음 살아 있는 사람에게 봇 권한(`host`)을 넘김. TV 모드는 `wakeLock`으로 잠들지 않게.
- **컵(그랑프리) 모드**: 대기실에서 «컵: 코스 4개 연속» 켜면 판마다 점수 누적(`TEAM_PTS` 재사용), 판 사이 30초 자동 카운트다운으로 다음 코스, 마지막에 종합 시상식. 교실 TV에 누적 순위표. 서버 `room.cup = {round, scores}`.
- **빠른 채팅·이모지**: 대기실·레이스 중 6개(«잘했어!» «다시!» 😆 😱 🔥 👏). 서버 `emote` 60ms 제한, 화면 위 말풍선 1.5초. 텍스트 입력은 없음(욕설 회피).
- **이름 필터**: `cleanName`에 한국어 금칙어 목록(교실 아케이드 `server.js`에 이미 있으면 재사용) — 있으면 «레이서N»으로 대체.
- **지연 표시**: 핑 200ms 넘으면 HUD 구석에 작은 안테나 아이콘.
- **치팅 최소 방어**: 서버가 15Hz 위치를 받을 때 «직전 위치에서 60m/s 초과 이동」이면 무시 + 3번이면 결승 무효. 20초 결승 거부(있음).

### 2-8. 코덱스가 열리면 (이미지 필요)  ★★

- **8방향 캐릭터 스프라이트**(앞·뒤·좌·우·대각 4): 지금 앞/뒤 2장이라 옆에서 보면 종이. 캐릭터당 1장 시트(4×2)로 뽑고 `tools/kart-sprites.py`로 잘라 `KartView.update`에서 카메라 각도로 8장 중 선택(각도 45° 단위, 좌우는 미러). 프롬프트 `prompts3.jsonl`처럼 마젠타 배경.
- **표정 3종**(기본·피격 놀람·승리): 피격 1.3초·시상식 때 교체.
- **자연스러운 상반신 초상화 10장**(`scratchpad/kart/prompts3.jsonl`, 재시도 루프가 돌고 있음) → 캐릭터 고르기·대기실·결과.
- **코스 썸네일**: K-POP 코스는 지금 키아트를 쓰고 있음 → 무대 그림.
- **로고**: «SUPERSTAR KART» 로고 이미지(지금은 글꼴).

### 2-9. 작지만 티 나는 것들 (틈틈이)

- 도로: 출발선 앞 «SUPERSTAR» 노면 글자, 코너 진입 «화살표 표지판」(인스턴스), 타이어 벽(토러스 3단) 코너 바깥.
- 관중석 지붕·깃발(흔들리는 셰이더) — 스탠드에 이미 관중이 있음.
- 결과 화면: 최고 랩타임, 순위 변동(▲2), 팀전 MVP.
- 설정: 소리 크기 슬라이더(BGM/효과 따로), 진동 켜기, 카메라 거리(가까이/보통/멀리).
- 접근성: 색약 대비 팀 색(빨강/파랑 + 무늬), 글자 크기 옵션.
- HUD 폰 세로: 순위 큰 숫자가 조이스틱과 겹치지 않는지 실기기로.

---

## 3. 이 세션에서 얻은 함정 (구현 세션이 꼭 볼 것)

- **도로 리본·연석 삼각형 방향**이 아래를 보면 안 보인다(`idx.push(a, a+1, a+2, a+1, a+3, a+2)`가 위쪽). 새 리본(다리·오버패스)도 같은 순서.
- **봇 드리프트**: 드리프트 중 실제 조향 = `dir*0.62 + 입력*0.48`, 도는 힘 1.28배 → AI는 원하는 조향에서 입력을 **역산**해야 한다(`ai.js`). 물리를 바꾸면 이 역산부터.
- **벽에서 방향을 매 걸음 트랙 쪽으로 돌리면** 벽에서 못 빠져나온다 — 벽 쪽으로 가고 있을 때만.
- **카메라 위치 스무딩**은 고속에서 10m 뒤처진다 → 위치는 붙이고 각도만 스무딩(지금 방식 유지).
- **코스 로딩**: 지형·소품 만들기 + `renderer.compileAsync` 뒤에 `t0`를 잡아야 카운트다운이 산다(혼자 모드는 준비 뒤 5.2초, 온라인은 서버 9초).
- **서버는 출발 20초 안 결승을 거부** → 검사에서 진행도 점프는 16초 뒤.
- **Render 프록시**: `ws.terminate()`해도 도배한 클라이언트는 close를 못 받는다(서버는 처리 중단). 검사 실패 1건은 이것.
- **코덱스 `codex-img` 동시 2**면 파일이 뒤바뀐다 → 동시 1, md5 중복 검사.
- **PIL `Image.fromarray`에 floodfill**은 조용히 무시 → `.copy()`.
- **브라우저 pane 스크린샷**은 pane이 숨으면 멈춘다 → 판정은 `test/kart/*.cjs`(GPU 플래그 필수).
- **폰 화질(q1)** 캡처를 꼭 볼 것 — 형님은 폰으로 본다. q1은 그림자·블룸·반사 물이 없다.
- **팀 깃발**을 운전자 뒤에 달면 내 캐릭터를 가린다.
- **setPointerCapture**는 합성 이벤트에서 던진다 → try/catch(있음).

## 4. 추천 작업 순서 (오퍼스 세션용)

1. `test/kart/README.md`대로 검사 환경 확인 → 2-1(a)(b)(c)(d) → 배포 → **형님 폰 실측**
2. 2-2 물리(슬립·충돌·슬립스트림) → `sim3.mjs`·직접 주행 → 배포
3. 2-3 아이템(뒤로 던지기·방패·무적·연출) → 2-7 재접속·봇 이관(교실용 필수)
4. 2-4 코스 장면(비치 개방·네온 오버패스부터) → 2-5 연출 → 2-6 소리
5. 2-7 나머지(컵·이모지) → 2-9 잔손질
6. 코덱스 열리면 2-8

한 항목 끝날 때마다: `node test/kart-server.test.js` + `pw-solo.cjs` 4코스 + `pw-multi.cjs` + `pw-tv.cjs` + 폰 캡처(`pw-look.cjs`) → push.

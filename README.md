# 교실 아케이드

학급 실시간 멀티플레이 미니게임 플랫폼.
선생님이 방을 열면 TV에 QR코드가 뜨고, 학생들은 폰으로 QR을 찍어 입장해서 다 같이 게임합니다.

- 게임 18종 (한 방 최대 40명)
  - **조이스틱**: 💣 폭탄 피하기 · 🧟 감염 술래잡기 · 🪙 동전 줍기(폭탄 밟으면 0원) · 💥 탄막 피하기 · 🪑 의자 뺏기 · ⭕ OX 서바이벌(몸으로 답하기)
  - **양손 조작**: 🏎️ 레이싱(핸들+부스터) · 🚀 갤러그(이동+발사) · 🥊 스모 밀치기(이동+대시) · 🏃 설원 러너(레인 이동+점프)
  - **탭·버튼**: 🦟 모기 잡기(화면 두드리기) · 🧸 인형 뽑기(액션 버튼)
  - **복불복**: 🪜 사다리 복불복 · 🏴‍☠️ 통아저씨(통 돌려 칼 꽂기) · ⏱️ 10초를 잡아라(시간 감각)
  - **입력**: 📝 낱말 빨리치기 · 🔤 초성 퀴즈(ㅇㅇㅅㅋㄹ → 아이스크림) · 🧠 퀴즈쇼(카훗식 4지선다)
- 로비의 게임 목록은 분류 탭(⚔️ 서바이벌 · 🏆 점수 경쟁 · 🧠 퀴즈·두뇌 · 🎲 복불복)으로 골라볼 수 있다
- 교사 화면(TV·PC)은 대형 2열 레이아웃, 학생 화면은 모바일 최적화. 효과음은 교사 화면에서만 재생(🔊 버튼으로 토글)
- 라이브: https://class-arcade-i6xj.onrender.com
- 기술: Node.js + WebSocket(ws) 서버 1개가 웹페이지 서빙과 게임 판정을 모두 담당
- 기획서: [PRD.md](PRD.md)

## 내 컴퓨터에서 실행 (테스트)

```bash
npm install
node server.js
```

브라우저에서 `http://localhost:3000` 접속 → 방 만들기.
같은 컴퓨터에서 학생 흉내를 내려면 탭을 하나 더 열고
`http://localhost:3000/?room=방코드&fresh=1` 로 접속한다.
(`fresh=1`은 같은 브라우저의 탭들이 서로 딴 사람으로 입장하게 하는 테스트 옵션)

## Render 무료 배포 (처음 하는 사람용)

Render는 서버를 무료로 돌려주는 서비스다. 카드 등록 없이 가입 가능.

### 1. 코드를 GitHub에 올린다
이 폴더가 GitHub 저장소로 올라가 있어야 한다. (Claude에게 "GitHub에 올려줘"라고 하면 됨)

### 2. Render 가입
1. https://render.com 접속 → **Get Started**
2. **GitHub로 로그인** 선택 (계정 연동 승인)

### 3. 서버 만들기
1. 대시보드에서 **New +** → **Web Service**
2. 방금 올린 저장소(class-arcade) 선택 → **Connect**
3. 설정 입력:
   - **Name**: `class-arcade` (원하는 이름)
   - **Region**: `Singapore` (한국에서 가장 가까움)
   - **Build Command**: `npm install`
   - **Start Command**: `node server.js`
   - **Instance Type**: **Free** 선택 ← 중요!
4. **Create Web Service** 클릭
5. 2~3분 기다리면 위쪽에 `https://class-arcade-xxxx.onrender.com` 주소가 생긴다.
   이게 완성된 서비스 주소다. 교실에서 이 주소로 접속하면 된다.

### 4. 이후 수정 반영
코드를 고쳐서 GitHub에 push하면 Render가 자동으로 다시 배포한다. 따로 할 일 없음.

## 무료 플랜 주의사항

- **15분간 아무도 안 쓰면 서버가 잠든다.** 다음 접속자는 깨어날 때까지 30초쯤 기다린다.
  → 수업 시작 전에 미리 한 번 접속해서 깨워두면 쾌적하다.
- 무료 한도는 월 750시간 — 서비스 1개면 한 달 내내 켜져 있어도 무료다.

## 운영 팁

- 교사 화면(TV)을 새로고침해도 방은 유지된다 (자동 복귀).
- 학생 폰이 잠기거나 새로고침돼도 같은 방 코드로 다시 들어가면 원래 자리로 복귀한다.
- 이름 장난치는 학생은 로비에서 이름 칩을 눌러 내보낼 수 있다.
- 방은 마지막 활동 후 30분이 지나면 자동 삭제된다.
- `서버주소/debug` 로 현재 방·참가자 현황을 볼 수 있다.

## 게임별 규칙 요약

| 게임 | 조작 | 규칙 |
|---|---|---|
| 💣 폭탄 피하기 | 조이스틱 | 빨간 원은 1초 뒤 폭발. 최후 1인 승 (최대 90초) |
| 🧟 감염 술래잡기 | 조이스틱 | 좀비에게 닿으면 나도 좀비(좀비가 8% 빠름). 60초 생존 |
| 🪙 동전 줍기 | 조이스틱 | 동전 1원·금화 3원. **💣폭탄 밟으면 모은 돈 전부 0원** |
| 🦟 모기 잡기 | 화면 탭 | 두드린 자리 반경의 모기를 잡음. 황금 모기 3점 |
| 🧸 인형 뽑기 | 액션 버튼 | 좌우로 움직이는 집게를 멈춰 인형을 집음(1·3·5점), 갈수록 빨라짐 |
| 🏎️ 레이싱 | 조이스틱+부스터 | 왼손 핸들 + 오른손 부스터(3회). S자 서킷, 물웅덩이·기름 함정. 3랩 완주 |
| 🚀 갤러그 | 조이스틱+발사 | 왼손 이동 + 오른손 발사(꾹 누르면 연사). 죽으면 부활 없음, 격추 수 순위 |
| 💥 탄막 피하기 | 조이스틱 | 사방에서 쏟아지는 탄막 회피. 한 발 즉사. 전멸하거나 시간이 끝날 때까지 진행 |
| 🥊 스모 밀치기 | 조이스틱+대시 | 서로 밀쳐서 링 밖으로 떨어뜨리기. 링이 점점 좁아짐. 최후 1인 승 |
| 🪑 의자 뺏기 | 조이스틱 | 음악이 멈추면 의자로 돌진! 못 앉으면 탈락, 의자는 라운드마다 감소 |
| 🪜 사다리 복불복 | 탭 | 8칸 중 하나 선택 → 사다리 타고 내려가 +10 황금·꽝·💣폭탄 결정. 5라운드 합산 |
| 🏴‍☠️ 통아저씨 | 드래그+탭 | 통을 360° 돌려 칼 꽂기. 위험 칸이면 해적이 펑! 튀어나오며 탈락. 위험 칸은 라운드마다 증가 |
| 🏃 설원 러너 | 조이스틱+점프 | 뒤에서 보는 3D 달리기. 얼음 블록은 점프, 기둥은 옆으로! 코인 8개면 ⚡피버 질주. 점수=거리+코인, 탈락 없음 |
| ⭕ OX 서바이벌 | 조이스틱 | 문장을 읽고 O구역/X구역으로 달려가 몸으로 답하기. 오답 탈락, 전원 오답이면 전원 부활 |
| ⏱️ 10초를 잡아라 | 화면 탭 | 랜덤 목표 시간(4~16초)이 나오고 시계가 가려짐 — 감으로 세다가 정확한 순간 탭. 오차 작을수록 고득점, 0.05초 안이면 ⚡보너스. 4라운드 |
| 🧠 퀴즈쇼 | 4색 버튼 | 카훗식 4지선다. 주제(스포츠·역사·연예·영화·음악·상식·믹스·✏️내문제)와 문제 수(5·10·15) 선택. 내문제는 "문제/정답/오답1/오답2/오답3" 형식으로 붙여넣기. 빨리 맞힐수록 보너스 |
| 📝 낱말 빨리치기 | 텍스트 | TV의 제시어를 그대로 입력. 5라운드, 순서대로 10·8·6…점 |
| 🔤 초성 퀴즈 | 텍스트 | 초성에 맞고 **사전(단어 목록)에 있는 단어만 정답**. 1~4라운드는 음식·동물·사물, 5라운드는 영화·노래 제목. 남이 먼저 쓴 단어는 불가 |

## 파일 구조

```
server.js          서버 전체 (방 관리 + 게임 판정 + 정적 파일 서빙)
public/index.html  화면 전체 (교사 TV 모드 + 학생 폰 모드)
package.json       의존성 (ws 하나뿐)
PRD.md             기획서
```
